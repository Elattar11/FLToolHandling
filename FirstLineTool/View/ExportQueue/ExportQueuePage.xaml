<Page x:Class="FirstLineTool.View.ExportQueue.ExportQueuePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FirstLineTool.View.ExportQueue"
      mc:Ignorable="d" 
      Loaded="Page_Loaded"
      
      FontFamily="{DynamicResource MaterialDesignFont}"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ExportQueuePage">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Total TextBlock -->
            <RowDefinition Height="Auto"/>
            <!-- ComboBoxes + Old Buttons -->
            <RowDefinition Height="Auto"/>
            <!-- New ComboBox + New Button -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
        </Grid.RowDefinitions>

        <!-- Row 0: Total Queue TextBlock -->
        <TextBlock x:Name="txtTotalQueue"
                   Grid.Row="0"
                   FontWeight="Bold"
                   FontSize="16"
                   Foreground="Black"
                   HorizontalAlignment="Center"
                   Margin="0,20,0,20"
                   Text="Total Queue: 0"/>

        <!-- Row 1: ComboBoxes القديمة + الأزرار -->
        <Grid Grid.Row="1" Margin="20,0,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <ComboBox x:Name="cbxConnections"
                          Width="200"
                          Margin="10,0,0,0"
                          SelectionChanged="cbxConnections_SelectionChanged"
                          IsEditable="True"
                          IsTextSearchEnabled="False"
                          StaysOpenOnEdit="True"
                          TextSearch.TextPath="Display"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          materialDesign:HintAssist.Hint="Select Queue"/>
                <ComboBox x:Name="cbxQueueQueries"
                          Width="200"
                          Margin="10,0,0,0"
                          IsEditable="True"
                          IsTextSearchEnabled="False"
                          StaysOpenOnEdit="True"
                          TextSearch.TextPath="Display"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          materialDesign:HintAssist.Hint="Select Query"/>
                <Button Content="Run Queue"
                        x:Name="btnExport"
                        Margin="10,0,0,0"
                        Click="btnExport_ClickAsync"
                        Foreground="White"
                        Background="#b9052f"
                        BorderBrush="Transparent"
                        materialDesign:ButtonAssist.CornerRadius="24"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Width="150"
                        Height="50"/>
                <Button Content="Export To Excel"
                        x:Name="btnExportToExcel"
                        Margin="10,0,0,0"
                        Click="btnExportToExcel_Click"
                        Foreground="White"
                        Background="#0f8a0f"
                        BorderBrush="Transparent"
                        materialDesign:ButtonAssist.CornerRadius="24"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Width="150"
                        Height="50"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Name="btnSettings" Style="{StaticResource topButton}" Click="btnSettings_Click">
                    <materialDesign:PackIcon Kind="Queue" Width="18" Height="18"/>
                </Button>
                <Button Name="btnQueries" Style="{StaticResource topButton}" Click="btnQueries_Click">
                    <materialDesign:PackIcon Kind="SqlQuery" Width="18" Height="18"/>
                </Button>

                <Button Name="btnRefresh" Style="{StaticResource topButton}" Click="btnRefresh_Click">
                    <materialDesign:PackIcon Kind="Refresh" Width="18" Height="18"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Row 2: ComboBox جديد + زرار جديد -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0,20,10">
            <ComboBox x:Name="cbxUsers" Width="200" Margin="10,0,10,0"
                      IsEditable="True"
                      
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      materialDesign:HintAssist.Hint="Select User"/>
            <Button x:Name="btnAssignTo" Content="Assign SRs" Width="150" Height="50"
                    Click="btnAssignTo_Click"
                    Background="#0055aa" Foreground="White" BorderBrush="Transparent"
                    materialDesign:ButtonAssist.CornerRadius="24"/>
        </StackPanel>

        <!-- Row 3: DataGrid -->
        <DataGrid x:Name="ResultsDataGrid"
                  Grid.Row="3"
                  RowHeight="24"
                  Margin="20,0,20,20"
                  AutoGenerateColumns="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeColumns="True"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  SelectionUnit="Cell"
                  SelectionMode="Extended"
                  GridLinesVisibility="All"
                  BorderBrush="#d0d0d0"
                  BorderThickness="1"
                  HorizontalGridLinesBrush="#e0e0e0"
                  VerticalGridLinesBrush="#e0e0e0"
                  AutoGeneratingColumn="ResultsDataGrid_AutoGeneratingColumn">

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="6"/>
                    <Setter Property="BorderThickness" Value="0.5"/>
                    <Setter Property="BorderBrush" Value="#d0d0d0"/>
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="SnapsToDevicePixels" Value="True"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border x:Name="CellBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="CellBorder" Property="Background" Value="#eaf3ff"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="CellBorder" Property="Background" Value="Black"/>
                                        <Setter TargetName="CellBorder" Property="BorderBrush" Value="#333333"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#bf072f"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Height" Value="35"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="BorderBrush" Value="White"/>
                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

        </DataGrid>

        <!-- Overlay Loader -->
        <Grid x:Name="OverlayLoader"
              Background="#88000000"
              Visibility="Collapsed"
              Grid.RowSpan="4">
            <ProgressBar Width="80" Height="80" IsIndeterminate="True" 
                         Style="{StaticResource MaterialDesignCircularProgressBar}" 
                         HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

    </Grid>


</Page>
