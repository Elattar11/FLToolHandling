<Window x:Class="FirstLineTool.View.LoadingScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FirstLineTool.View"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="White"
        AllowsTransparency="True"
        Topmost="True"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="LoadingScreen" Height="450" Width="800">
    <Border CornerRadius="12" Background="White" BorderBrush="#b0b0b0" BorderThickness="1" Padding="20">
        <Grid>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" >
                <!-- Progress Ring -->
                <ProgressBar IsIndeterminate="True" Width="200" Height="20" Foreground="#b9052f" />

                <!-- Text -->
                <TextBlock Text="Welcome to FL Tool Handling, please wait..." 
                           FontSize="16" 
                           FontWeight="Bold" 
                           Foreground="#333333"
                           Margin="0,20,0,0"
                           HorizontalAlignment="Center" />
            </StackPanel>

            <!-- CopyRights أسفل -->
            <TextBlock Text="© 2025 Mohamed.AMAAhmed. All rights reserved."
                       FontSize="12"
                       Foreground="#999999"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Bottom"
                       Margin="0,0,0,10"/>
        </Grid>
    </Border>
</Window>
