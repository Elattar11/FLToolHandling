<Window x:Class="FirstLineTool.View.Dashboard.DashboadrQuery"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FirstLineTool.View.Dashboard"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
        FontFamily="{DynamicResource MaterialDesignFont}"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        AllowsTransparency="True"
        Loaded="Window_Loaded"
        Background="Transparent"
        Title="DashboadrQuery" Height="300" Width="800">
    <Border Background="White" CornerRadius="10" 
        BorderBrush="#bf072f"
        BorderThickness="1"
        MouseDown="Border_MouseDown">

        <Grid>
            <!-- زرار Close فوق اليمين -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0 10 10 0">
                <Button Name="btnClose" Style="{StaticResource topButton}" Click="btnClose_Click">
                    <Icon:PackIconMaterial Kind="Close" Width="12" Height="12"/>
                </Button>
            </StackPanel>

            <!-- محتوى الفورم -->
            <StackPanel Margin="20,0,20,0" VerticalAlignment="Center">

                <!-- GroupBox -->
                <GroupBox Header="Query Dashboard"
                      HorizontalAlignment="Stretch"
                      BorderBrush="Gray"
                      BorderThickness="1"
                      Background="White"
                      Padding="10"
                      SnapsToDevicePixels="True" Height="195">

                    <GroupBox.Resources>
                        <Style TargetType="GroupBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="GroupBox">
                                        <Grid>
                                            <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="3" Background="White" />
                                            <DockPanel Margin="10,7,10,10">
                                                <Label DockPanel.Dock="Top"
                                                   Content="{TemplateBinding Header}"
                                                   Background="White"
                                                   Foreground="Black"
                                                   FontWeight="Bold"
                                                   Margin="10,-17,0,5"/>
                                                <ContentPresenter Margin="0,5,0,0"/>
                                            </DockPanel>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupBox.Resources>

                    <!-- لفنا المحتوى كله داخل StackPanel عشان GroupBox يكون Content واحد -->
                    <StackPanel>

                        <!-- TextBox واحد Multiline -->
                        <Grid Height="100">
                            <!-- تحدد ارتفاع ثابت -->
                            <TextBox x:Name="txtDashboardQuery"
             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
             materialDesign:HintAssist.Hint="Enter server settings"
             BorderBrush="Black"
             BorderThickness="0 0 0 1.5"
             AcceptsReturn="True"
             TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto"
             HorizontalScrollBarVisibility="Auto"/>
                        </Grid>

                        <!-- زرار Update -->
                        <Button x:Name="btnUpdate"
                            Content="Update"
                            Width="120" Height="35"
                            Background="#076ebf"   
                            BorderBrush="Transparent"
                            Foreground="White"
                            HorizontalAlignment="Center"
                            Margin="0,15,0,0"
                            Click="btnUpdate_Click"/>

                    </StackPanel>
                </GroupBox>

            </StackPanel>
        </Grid>
    </Border>

</Window>
